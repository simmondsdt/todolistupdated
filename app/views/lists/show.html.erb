<h3><%= @list.title %></h3>
  <%= link_to 'Edit', edit_list_path(@list) %> --
  <%= link_to 'Delete', list_path(@list), method: :delete, data: {confirm: "Are you sure you would like to delete this list?"} %> --
  <%= link_to 'Add an Item', new_list_item_path(@list) %>
<hr />

<%= render :partial => 'items/index' %>
